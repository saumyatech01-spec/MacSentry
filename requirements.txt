# MacSentry — Python Dependencies
# Install with: pip install -r requirements.txt

# Core runtime monitoring
psutil>=5.9.0

# PDF report generation
reportlab>=4.0.0

# Optional: enhanced HTTP for CVE API calls
requests>=2.31.0

# Testing
pytest>=7.4.0

# Optional: packaging as standalone app
# pyinstaller>=6.0.0


# --- AI Suggestion API (FastAPI microservice on port 5002) ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0          # async HTTP client (used by OllamaProvider)

# Gemini provider — swap-ready; replace with ollama/vllm later
google-generativeai>=0.7.0
